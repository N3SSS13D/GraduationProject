# 基于MCU的智能LED图示条幅的设计-计划书

## 基础要求

### 1.LED驱动显示图像

#### 1.1硬件方案

1. 16路LED，每路由64个WS2812B驱动的LED串联组成；
2. 使用mos管作开关控制各路的开关，通过4-16译码驱动，依次显示每行，减小功耗；（待验证可行性）
3. 使用一个IO口输入控制信号；
4. 模块化设计，每块为8*8，可拼接。

#### 1.2软件方案

1. 使用SPI+DMA/PWM+DMA实现基本驱动
2. 实现逐行点亮显示完整图像
3. 设置缓冲区显示不同图像
4. 使用OS进行任务调度

### 2.使用语音模块进行语音识别